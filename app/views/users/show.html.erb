
<h2>hello again <%= current_user.name %>, you're on users#show</h2>

<h3>All the parkings:</h3>
<ul>
	<% @parkings.each do |parking| %>
		<li>
			<h3><%= parking.title %></h3>
			<p>Price : <%= parking.price %> €</p>
			<hr>
		</li>
	<% end %>
</ul>
